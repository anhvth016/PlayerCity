export enum ROLE {
  USER = 1,
  ADMIN = 2
}
export enum LANGUAGE {
  vi = 'Vietnamese',
  en = 'English',
  jp = 'Japanese'
}

export enum SOCKET_ACTION {
  CLIENT_MESSAGE_PUSH = 'client/message/push',
  CLIENT_MESSAGE_RECEIVE = 'client/message/receive'
}